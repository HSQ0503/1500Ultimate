# 1500 Ultimate

## Run locally

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). The demo defaults to Pro; append `?plan=free`, `?plan=blueprint`, or `?plan=pro` to review entitlements.
